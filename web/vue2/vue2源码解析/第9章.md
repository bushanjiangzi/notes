## v-model
```
let vm = new Vue({
  el: '#app,
  template: '<div>' + 
  '<p>Message is {{ message }</p>}' +
  '<input ' +
  ':value="message=$event.target.value" ' +
  'placeholder="edit me">' +
  '</div>',
  data() {
    return {
      message: ''
    }
  }
})
```